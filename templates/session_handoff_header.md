# SESSION_HANDOFF — {{PROJECT_NAME}}

> Registry of handoffs. `[closed-pending] 🟢` awaits receipt;
> `[closed] ✅` is consumed and may be purged.
> Resume: `/handoff resume <code>` · Purge: `/handoff purge`
